[project]
name = "watchdog"
version = "0.1.0"
description = "Zentraler Statusdienst für externe Tools"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=2.3.0",
    "apscheduler>=3.10.0",
]
